<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>상품 추가</title>
</head>
<body>
    <h1><%= farm.name %></h1>
    <h2>상품 추가하기</h2>
    <form action="/farms/<%=farm._id%>/products" method="POST">
        <label for="name">상품 이름</label>
        <input type="text" name="name" placeholder="상품 이름" id="name">
        <label for="price">상품 가격</label>
        <input type="number" name="price" placeholder="상품 가격" id="price">
        <label for="category">상품 종류</label>
        <select name="category" id="category">
            <% for(let category of categories) { %>
                <option value="<%=category%>"><%=category%></option>
            <% } %>
        </select>
        <button>추가하기</button>
    </form>
    <a href="/farms/<%=farm._id%>">돌아가기</a>
</body>
</html>